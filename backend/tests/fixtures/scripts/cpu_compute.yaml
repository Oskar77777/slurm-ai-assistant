node: n001
type: cpu
script: |
  #!/bin/bash
  #SBATCH --job-name=cpu_compute
  #SBATCH --output=output_%j.txt
  #SBATCH --nodelist={node}
  #SBATCH --cpus-per-task=16
  #SBATCH --time=02:00:00
  #SBATCH --mem=64G

  python compute_heavy.py
