node: g002
type: gpu
script: |
  #!/bin/bash
  #SBATCH --job-name=inference
  #SBATCH --output=output_%j.txt
  #SBATCH --nodelist={node}
  #SBATCH --gres=gpu:1
  #SBATCH --time=01:00:00
  #SBATCH --mem=16G

  module load cuda/12.0
  python inference.py --model checkpoint.pt
